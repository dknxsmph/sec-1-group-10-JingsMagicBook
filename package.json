{
  "name": "jings-magic-book",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "dev": "concurrently \"vite\" \"npm run backend\"",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server --watch ./data/db.js --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "vue": "^3.2.25",
    "vue-router": "^4.0.14"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.2.0",
    "concurrently": "^7.0.0",
    "vite": "^2.8.0"
  }
}